# Ownership index — shared facts

> Facts more than one spec depends on. A spec either **points here** or defines the fact
> locally. Silently redefining a shared fact in a second spec is a Gate 1 miss
> (`.agent/rules/governance.md` DoR).

**Maintained with** the spec that first recorded the fact. Later specs cite the row ID.

| ID | Fact | Owner spec | Cited by |
| --- | --- | --- | --- |
| OWN-01 | Token **subject** is the portal employee id (`employee_id`, `assigned_party_ref`). A caller-supplied employee id in body, query or path is never used for authorisation. | `internal-transfer-request` (AC13, AC20); architecture AuthN/AuthZ | approval-chain BR6/AC6; notifications BR5 |
| OWN-02 | Unauthorised access to another principal's request returns **404** `request-not-found`, not 403. | `internal-transfer-request` AC13 | approval-chain AC6 |
| OWN-03 | Current line manager acts on `MANAGER_RELEASE`; receiving manager on `MANAGER_ACCEPT`; order is release then accept then HR. | BRD-001 OQ-01, OQ-02 | request stage plan; approval-chain BR1 |
| OWN-04 | HR validation is authorised by IdP role `HR_BUSINESS_PARTNER`, not by a named assignee (v1). | BRD-001 BR13; approval-chain BR6 A2/A3 | notifications HR fan-out |
| OWN-05 | Transfer reason text is visible to the owning employee and HR Business Partner only — not to either manager. Never in logs, events or notification payloads. | `internal-transfer-request` AC16; BRD-001 OQ-12 (**Resolved 2026-09-11, Product v1**) | approval-chain BR5/AC7; notifications matrix |
| OWN-06 | Downstream fulfilment callbacks authenticate with HMAC, not an employee OIDC token. | BRD-001 BR14 | downstream AC8, AC11 |
| OWN-07 | Portal existing SSO/OIDC is reused. This programme does not add login, registration or identity administration. | BRD-001 KD-07 | all four transfer specs |
| OWN-08 | **HR Operations** is the operational owner of a failed fulfilment. After a fulfilment stage reports `FAILED`, the request rests in `FULFILMENT` and the transfer is closed out **off-portal** — v1 has no automatic and no portal-driven resume, retry or "mark complete". | `internal-transfer-downstream-orchestration` BR10/AC17; BRD-001 OQ-20 (**Resolved 2026-09-11, Product v1 — portal resume deferred**), OQ-06 | downstream AC14, A5; notifications AC15 |
| OWN-09 | Domain events in this journey are named `employee.transfer.<past-tense>.v<major>` — the **version suffix is mandatory**, and a breaking payload change means a new suffix, never an in-place edit. Canonical list in `internal-transfer-notifications` § _Event naming and versioning_. All four specs now comply: `internal-transfer-request` adopted the suffix in its v1.3 (2026-09-11, its G1-F04), closing the four-way inconsistency. | `internal-transfer-request` (aggregate owner) | notifications BR6/AC16; request § _Domain Events Emitted_, AC26; approval-chain and downstream event lists |
| OWN-10 | **The authoritative request/stage state and transition contract** — every request status, every stage status (including `COMPENSATION_REQUESTED` / `COMPENSATED` / `COMPENSATION_FAILED` / `FAILED`), every legal transition, and which spec performs each one — is the table in `internal-transfer-request` § _Authoritative State and Transition Contract_. Employee-facing `statusDisplay` labels for those statuses are owned there. Any `(from, trigger)` pair absent from it is refused with 409 `invalid-state-transition`. `IN_PROGRESS` (never `PENDING`) means "current and awaiting action". `SUBMITTED` is a history/audit event type, **not** a request status. Two request statuses are defined but have no producer in v1: `CANCELLED` and `DISCARDED` (**Product 2026-09-11:** no discard endpoint; `CANCELLED` unreachable — OQ-06). | `internal-transfer-request` AC9, AC11, AC27 (Gate 1 G1-F01, G1-F02, G1-F03) | approval-chain (stages 1–3 and its status transitions); downstream (stages 4–7, its transition matrices); notifications _Transition coverage_ |
| OWN-11 | **The request aggregate carries one monotonic `version`.** Every mutation from any spec increments it, and concurrency is optimistic on that single value: first commit wins, the loser is refused and never merged. A withdrawal losing to an approval gets `version-conflict` while the request is still withdrawable, and `withdrawal-window-closed` once it has reached `FULFILMENT`. | `internal-transfer-request` BR13, AC2, AC23 (Gate 1 G1-F08) | approval-chain (decision endpoints mutate the same aggregate); downstream (stage reports do too) |
| OWN-12 | In employee-facing "pending with" and stage `assignedPartyName`, a **named person is shown only when that person is the owning employee's current line manager**. Every other stage shows the role only. | `internal-transfer-request` AC11, API04; BRD-001 OQ-11 (**Resolved 2026-09-11, Product v1**) | approval-chain BR6 (HR remains a role, OWN-04); notifications does not disclose names |

